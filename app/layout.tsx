import './globals.css'

// The [locale] layout provides html/body — this root layout just passes through
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
